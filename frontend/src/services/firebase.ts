// LOCAL MOCK — no Firebase needed
// When you have Firebase credentials, replace this file with the real version from firebase.real.ts

export const auth = null;
export const googleProvider = null;
export const db = null;
